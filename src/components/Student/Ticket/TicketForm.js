import React from 'react';
import {
  Form,
} from 'antd';

class Ticket extends React.Component {
  render() {
    return (
      <div>this is ticket form</div>
    );
  }
}

export default Form.create()(Ticket);

